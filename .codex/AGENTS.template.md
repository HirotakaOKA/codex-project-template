# AGENTS.md

## 目的

このリポジトリでは、`<主要技術>`を使用して`<プロダクト概要>`を開発する。
Codexは、既存の仕様・設計・実装を尊重し、小さく検証可能な単位で変更すること。

## 作業開始時

1. ユーザーの依頼から目的、完了条件、対象範囲を整理する。
2. 作業に直接必要な参照文書だけを読む。
3. 変更前に既存実装、関連テスト、依存関係を確認する。
4. 不明点は推測で仕様化せず、前提または未解決事項として明示する。

## 基本方針

- 指示された範囲を超える変更を避ける。
- 既存の命名、構成、設計パターンを優先する。
- 変更はレビューしやすい単位に分割する。
- 挙動変更には、可能な限りテストを追加または更新する。
- 依存パッケージの追加、公開APIの変更、大規模な構造変更は慎重に扱う。
- 仕様、設計、実装が矛盾する場合は、推測で統一せず証拠と矛盾点を報告する。

## 参照文書

すべてを毎回読む必要はない。作業に関係する文書だけを参照すること。
存在しない文書やパスを推測して作成しないこと。

- プロジェクト概要・実行方法: `README.md`
- Git・変更管理: `<rules/git-workflow.md>`
- 長時間・複数工程タスク: `<rules/long-running-task.md>`
- 要件・機能仕様: `<docs/requirements.md>`
- UI・画面仕様: `<docs/ui-design.md>`
- 技術構成・依存追加: `<docs/development/tech-stack.md>`
- アーキテクチャ・配置規則: `<docs/development/architecture.md>`
- コーディング・テスト規約: `<docs/development/coding-style.md>`

## 検証

変更内容に応じて、以下から必要な検証を実行する。

```text
<format-command>
<lint-command>
<build-command>
<test-command>
```

- 実行できなかった検証は、未実施理由とともに報告する。
- 既存失敗と今回の変更による失敗を区別する。
- 検証を通すためだけに、無関係なコードやテストを変更しない。

## 完了報告

完了時は、少なくとも以下を簡潔に報告する。

- 変更した内容
- 変更した理由
- 影響範囲
- 実行した検証と結果
- 未解決事項または残存リスク

## 保護対象

以下を変更する場合は、必要性と影響を明示し、ユーザーの明示的な指示がない限り変更しない。

- `AGENTS.md`
- `README.md`
- `.gitignore`
- `.codex/`
- `.agents/`
- `skills/`
- `rules/`
- `<CI/CD設定ファイル>`
- `<デプロイ・インフラ設定>`

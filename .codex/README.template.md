# <Project Name>

`<Project Name>`は、`<主要技術>`を使用して`<利用者・目的>`向けに`<提供価値>`を実現するプロジェクトです。

## リポジトリの目的

- `<目的1>`
- `<目的2>`
- `<目的3>`
- Codexが必要な設計文書を参照し、安全に実装・検証できる状態を保つ

詳細な要件は[`<要件・仕様書のパス>`](<要件・仕様書のパス>)を参照してください。

## 技術構成

- `<言語・ランタイム>`
- `<アプリケーションフレームワーク>`
- `<UI・API・データストア>`
- `<テストフレームワーク>`
- `<主要な外部サービス>`

技術選定と制約は[`<技術構成文書のパス>`](<技術構成文書のパス>)を参照してください。

## 必要環境

- `<対応OS>`
- `<SDK・ランタイム>`
- `<IDE・エディター>`
- Git
- `<その他の必要ツール>`

## セットアップ

```powershell
# リポジトリ取得
git clone <repository-url>
cd <repository-directory>

# 依存関係の復元
<restore-command>

# ビルドとテスト
<build-command>
<test-command>
```

環境変数や秘密情報が必要な場合は、[`<環境設定文書のパス>`](<環境設定文書のパス>)を参照してください。
秘密情報をリポジトリへコミットしないでください。

## ローカル実行

```powershell
<run-command>
```

`<起動後の確認方法やURL>`

## 主なコマンド

```powershell
# フォーマット
<format-command>

# 静的解析
<lint-command>

# ビルド
<build-command>

# 全テスト
<test-command>

# 発行・パッケージ作成
<publish-command>
```

使用しない項目は削除してください。

## ディレクトリ構成

```text
src/                 # 製品コード
├─ <component-a>/    # <責務>
├─ <component-b>/    # <責務>
└─ <component-c>/    # <責務>

tests/               # 自動テスト
docs/                # 要件・設計・運用文書
rules/               # 必要時に参照する詳細ルール
AGENTS.md             # Codexが常時参照する基本指示
README.md             # 人間向けの概要と導入手順
```

実際の構成に合わせ、存在しないディレクトリは記載しないでください。

## 開発ドキュメント

- [`<要件・機能仕様>`](<path>): `<文書の役割>`
- [`<UI・画面仕様>`](<path>): `<文書の役割>`
- [`<技術構成>`](<path>): `<文書の役割>`
- [`<アーキテクチャ>`](<path>): `<文書の役割>`
- [`<コーディング規約>`](<path>): `<文書の役割>`
- [`<Git運用ルール>`](<path>): `<文書の役割>`

## 開発方針

- `<重要な設計原則1>`
- `<重要な設計原則2>`
- `<重要な設計原則3>`
- 変更後は`<必須検証コマンド>`を成功させる
- 指示されていない大規模変更や依存追加を避ける
- 仕様と実装が矛盾する場合は、推測で修正せず矛盾を記録する

## 開発の進め方

1. Issueまたはタスクで目的と完了条件を明確にする
2. 作業範囲と関連文書を特定する
3. 既存実装とテストを確認する
4. 小さな単位で実装し、必要なテストを追加する
5. ビルド、テスト、動作確認を行う
6. 変更理由と検証結果をコミットまたはPull Requestへ残す

## デプロイ

`<デプロイ方式の概要>`

```powershell
<deploy-or-publish-command>
```

デプロイ前に、`<必要な検証>`を確認してください。
詳細は[`<デプロイ手順書のパス>`](<デプロイ手順書のパス>)を参照してください。

## ライセンス

`<ライセンスまたは社内利用限定などの扱い>`
